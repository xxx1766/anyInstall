# Zsh & Oh My Zsh 一键安装 (Debian/Ubuntu)

安装内容：
- Zsh
- Oh My Zsh
- 插件：`git` / `z` / `zsh-autosuggestions` / `zsh-syntax-highlighting`

## 使用方法

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/xxx1766/anyInstall/main/zsh/install.sh)
```

> 需要 root 或 sudo 权限。安装完成后执行 `zsh` 或重新连接 SSH 即可生效。
